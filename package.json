{
  "name": "nodeping",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "ELECTRON_ENV=production electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ansi-colors": "^3.2.4",
    "highcharts": "^7.0.3",
    "electron-reload": "^1.4.0"
  },
  "devDependencies": {
    "electron": "^4.1.3",
    "electron-packager": "^13.1.1"
  }
}
